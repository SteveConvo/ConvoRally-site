export const metadata = { title: "Book a Demo — ConvoRally", description: "See if ConvoRally fits your workflow. Book a short walkthrough." };
export default function Layout({ children }) { return children; }
