export const metadata = { title: "ConvoRally for Construction & Custom Trades", description: "Track conversations, approvals, changes, and milestone payments without losing the trail." };
export default function Layout({ children }) { return children; }
